name: Build Panda Android Preview APK

on:
  workflow_dispatch: {}

jobs:
  build_android_preview:
    name: Build Android Preview APK
    type: build
    params:
      platform: android
      profile: preview
